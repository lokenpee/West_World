# StoryWeaver / WestWorld 项目整体流程说明

本文档用于快速理解当前插件工程的整体结构、主要文件职责，以及从用户输入到演员 AI 输出之间的完整文件流转顺序。

## 1. 项目定位

本项目是一个 SillyTavern 第三方插件，当前品牌名为 `WestWorld`，历史名为 `StoryWeaver`。

核心能力分两条主线：

1. `TXT 转世界书`
   - 用户导入 TXT 小说。
   - 插件按章节或字数切分为章节队列。
   - 调用 AI 抽取世界书条目、故事大纲、章节节拍等结构化数据。
   - 支持导出 SillyTavern 世界书、角色卡、工程包。

2. `导演 -> 演员`
   - 用户正常在酒馆聊天。
   - 插件在生成前拦截本轮上下文。
   - 导演 AI 根据当前章节、当前节拍、用户输入、最近正文，输出一个 JSON 判定。
   - 插件把导演 JSON 转成“导演给演员的执行单”，注入到演员 AI 的 system 消息里。
   - 演员 AI 根据执行单输出剧情正文。

## 2. 根目录结构

```text
StoryWeaver/
├─ index.js
├─ manifest.json
├─ drawer-component.html
├─ style.css
├─ README.md
├─ AGENTS.md
├─ preview(1).html
├─ 西部世界v1.0*.json
└─ txtToWorldbook/
   ├─ main.js
   ├─ app/
   ├─ core/
   ├─ infra/
   ├─ services/
   ├─ ui/
   └─ styles/
```

## 3. 根目录文件职责

### `manifest.json`

SillyTavern 插件清单。声明插件名称、入口、版本等元信息。SillyTavern 根据它加载插件。

### `index.js`

整个插件在 SillyTavern 中的主入口。

主要职责：

- 注册抽屉按钮和插件面板。
- 加载 `drawer-component.html`。
- 初始化插件设置。
- 懒加载 `txtToWorldbook/main.js`。
- 暴露 `window.WestWorldTxtToWorldbook` 相关能力。
- 监听 SillyTavern 生成事件。
- 在用户发送消息、生成开始、生成结束等时机调用导演服务。

导演链路入口主要在这里：

- 生成前：
  - 调用 `txtToWorldbook` 模块 API。
  - 执行 `runDirectorBeforeGeneration(eventData)`。
  - 给演员 AI 注入导演执行单。

- 生成后：
  - 调用 `handleDirectorAfterGeneration(eventData)`。
  - 更新节拍完成提示或状态。

### `drawer-component.html`

插件抽屉按钮和外层简单 UI 模板。

### `style.css`

根插件样式，主要服务抽屉和入口层。

### `README.md`

用户向说明文档。

### `AGENTS.md`

项目维护经验记录。里面记录了编码、中文、BOM、PowerShell 等坑。

### `preview(1).html`

旧版或预览用的静态 HTML。当前主代码以 `txtToWorldbook/` 下模块化文件为准。

### `西部世界v1.0*.json`

世界书、角色卡或导出数据样例/工作文件，不属于插件运行时代码。

## 4. `txtToWorldbook` 总体结构

```text
txtToWorldbook/
├─ main.js                  # TXT 转世界书模块入口与装配中心
├─ app/                     # 应用装配层：把 core/services/ui 组装起来
├─ core/                    # 常量、状态、工具、日志、运行时基础设施
├─ infra/                   # 基础设施：API 调用、IndexedDB、模态框工厂等
├─ services/                # 业务服务层：导入、处理、导演、导出、合并等
├─ ui/                      # UI 层：面板、弹窗、视图、事件绑定
└─ styles/                  # 独立样式文件
```

## 5. `txtToWorldbook/main.js`

这是 `TXT 转世界书` 模块的内部入口和装配中心。

主要职责：

- 导入所有核心模块、服务、UI 组件。
- 创建 `AppState` 和 `MemoryHistoryDB`。
- 创建 API、解析、世界书、处理、导演等服务。
- 创建 UI 视图和模态框运行时。
- 建立大量兼容层函数，保持旧代码调用方式还能工作。
- 最终导出公共 API 给 `index.js` 使用。

简单理解：

```text
index.js
  -> import('./txtToWorldbook/main.js')
  -> main.js 创建 AppState、services、ui
  -> main.js 导出 getTxtToWorldbookApi()
```

## 6. `core/` 文件职责

### `core/constants.js`

项目默认常量集中地。

包含：

- 插件版本。
- 默认章节正则。
- 默认世界书分类。
- 默认提示词：
  - 世界书抽取提示词。
  - 大纲提示词。
  - 风格提示词。
  - 合并提示词。
  - 导演框架提示词 `defaultDirectorFrameworkPrompt`。
  - 导演注入演员提示词 `defaultDirectorInjectionPrompt`。
- 默认设置 `defaultSettings`。

导演相关重点：

- `defaultDirectorFrameworkPrompt`
  - 发给导演 AI。
  - 目标是让导演 AI 输出 JSON。

- `defaultDirectorInjectionPrompt`
  - 发给演员 AI。
  - 由导演 JSON 渲染成执行单后注入。

### `core/state.js`

创建初始 `AppState`。

`AppState` 是全项目共享状态，包含：

- `file`
  - 当前 TXT 文件信息。
  - 小说名、hash 等。
- `memory`
  - 章节队列。
  - 起始处理位置。
  - 多选状态。
- `worldbook`
  - 生成出的世界书条目。
  - 分卷数据。
- `settings`
  - API、提示词、导演开关、自定义分类等设置。
- `processing`
  - 当前处理状态、任务集合、停止标记。
- `experience`
  - 章节体验模式状态。
  - 当前章节、当前节拍、导演上次判定等。
- `ui`
  - UI 临时状态。

### `core/utils.js`

通用工具。

主要包括：

- Token 粗估。
- 中文数字转换。
- 条目自然排序。

### `core/errorHandler.js`

统一用户提示和错误展示。

常见用途：

- `showUserError`
- `showUserSuccess`
- 包装 confirm / alert 类操作。

### `core/logger.js`

日志封装。

常见用途：

- `Logger.info`
- `Logger.warn`
- `Logger.error`

### `core/fileUtils.js`

文件读取相关工具。

负责：

- 读取 TXT。
- 编码检测。
- hash 计算。
- 文件内容标准化。

### `core/runtime.js`

运行时基础工具。

包含：

- `Semaphore`
  - 控制并发。
- `PerfUtils`
  - 防抖、节流等性能工具。
- `TokenCache`
  - Token 估算缓存。

## 7. `infra/` 文件职责

### `infra/apiCaller.js`

底层 API 请求封装。

负责：

- 对 OpenAI 兼容接口、Gemini、Anthropic 等做请求格式适配。
- 流式或非流式响应读取。
- 请求错误处理。

更高层的 `services/apiService.js` 会调用它。

### `infra/memoryHistoryDB.js`

IndexedDB 持久层。

负责保存：

- 任务快照。
- 章节队列。
- 世界书结果。
- Roll 历史。
- 处理状态。

典型用途：

- 页面刷新后恢复任务。
- 回滚某章结果。
- 导入导出工程包。

### `infra/modalFactory.js`

通用模态框工厂。

用于创建：

- 世界书预览弹窗。
- 历史记录弹窗。
- 搜索替换弹窗。
- 合并弹窗。
- 章节内容弹窗。

### `infra/eventDelegate.js`

事件委托工具。

用于减少大量 DOM 绑定，提高 UI 事件处理性能。

## 8. `app/` 文件职责

`app/` 是装配层，不主要写业务规则，而是把 `core / services / ui` 串起来。

### `app/createApp.js`

创建应用上下文：

```text
createInitialAppState()
createMemoryHistoryDB()
```

返回：

- `AppState`
- `MemoryHistoryDB`

### `app/createCoreServices.js`

创建核心服务：

- `promptService`
- `parserService`
- `apiService`
- `worldbookService`
- `tokenMetricsService`
- `exportFormatService`
- 懒加载：
  - `processingService`
  - `rerollService`
  - `rerollModals`

### `app/createFeatureServices.js`

创建功能型服务：

- 条目配置弹窗。
- 历史记录视图。
- 搜索弹窗。
- 替换弹窗。
- 帮助弹窗。
- 清理和替换服务。
- 导入合并服务。
- 工程包保存/恢复服务。
- 导出服务。
- 合并工作流服务。

### `app/createFeatureServicesConfig.js`

把 `main.js` 中的大量依赖整理成 `createFeatureServices()` 需要的参数。

作用是降低 `main.js` 的装配复杂度。

### `app/createMainBindings.js`

创建兼容别名、占位函数、公共 API 配置。

用于让旧代码仍能通过老函数名调用新模块。

### `app/createRuntimeBridges.js`

把服务方法重新包装成运行时绑定。

例如：

- `handleStartConversion`
- `handleStartDirectorConversion`
- `showHistoryView`
- `exportToSillyTavern`
- `showAliasMergeUI`

### `app/createShellRuntime.js`

创建 UI 外壳运行时。

负责装配：

- 文件导入服务。
- 设置持久化服务。
- 分类轻量设置服务。
- 条目配置服务。
- 模态框生命周期。
- 模态框控制器。
- 模态框事件绑定器。
- 模态框运行时 facade。

### `app/createShellRuntimeConfig.js`

整理创建 UI 外壳所需的参数。

其中会把 UI 事件绑定需要的函数都塞给 `modalEventBinder`。

### `app/publicApi.js`

导出给外部使用的 API。

外部一般通过：

```js
window.WestWorldTxtToWorldbook
```

调用：

- 打开面板。
- 获取世界书。
- 获取章节队列。
- 触发导演生成前/生成后处理。

### `app/runtimeActionsFacade.js`

把 UI 按钮动作包装为更稳定的接口。

例如：

- 开始转换。
- 开始导演切拍。
- 清理标签。
- 打开条目配置。

## 9. `services/` 文件职责

### `services/apiService.js`

高层 API 服务。

负责：

- 根据设置选择 API 模式。
- 酒馆 API 调用。
- 自定义 API 调用。
- 主 AI 和导演 AI 的不同配置。
- 生成 OpenAI / Gemini / Claude 格式请求体。
- `callAPI`
- `callDirectorAPI`

调用链：

```text
processingService / directorService
  -> apiService.callAPI / callDirectorAPI
  -> infra/apiCaller.js 或 fetch
```

### `services/promptService.js`

提示词构建服务。

用于世界书抽取、重 Roll 等处理场景。

### `services/parserService.js`

AI 返回内容解析服务。

负责：

- 从 AI 文本中提取 JSON。
- 修复常见 JSON 格式错误。
- 标准化世界书条目结构。

### `services/processingService.js`

TXT 转世界书的主处理服务。

负责：

- 顺序处理章节队列。
- 并行处理章节队列。
- 调用 AI 抽取世界书。
- 生成章节大纲和节拍。
- 保存进度。
- 更新 UI 进度。
- 失败重试。
- 暂停和停止。

典型流转：

```text
用户点击开始
  -> runtimeActionsFacade.handleStartConversion()
  -> processingService.handleStartProcessing()
  -> processMemoryChunk()
  -> promptService 构造提示词
  -> apiService.callAPI()
  -> parserService.parseAIResponse()
  -> worldbookService 合并条目
  -> MemoryHistoryDB 保存历史
  -> UI 更新队列和世界书预览
```

### `services/directorService.js`

导演功能核心。

这是“用户输入到演员 AI 输出”链路中最关键的文件之一。

主要职责：

1. 读取当前章节和当前节拍。
2. 从真实聊天和生成上下文中提取干净的用户输入、AI 正文尾部、最近正文。
3. 构造导演 AI 请求 prompt。
4. 调用导演 API。
5. 解析导演返回 JSON。
6. 补全或修正导演判定。
7. 根据导演判定渲染演员执行单。
8. 把执行单注入到 `eventData.chat` 开头。
9. 记录导演调试三段链路：
   - 发给导演 AI API 的提示词。
   - 导演 AI API 原始返回。
   - 最终注入演员 AI 的提示词。

关键函数：

- `runDirectorBeforeGeneration(eventData)`
  - 生成前入口。
  - 构建导演请求。
  - 注入演员执行单。

- `handleDirectorAfterGeneration(eventData)`
  - 生成后入口。
  - 处理节拍完成提示。

- `buildDirectionContext(...)`
  - 计算起笔锚点。
  - 判断新入节拍、节拍中段续写、大跳转。

- `buildDirectorPrompt(...)`
  - 生成发给导演 AI 的 prompt。

- `buildInjection(...)`
  - 把导演 JSON 转成演员执行单。

- `stripExistingDirectorInjection(chat)`
  - 清除旧的导演注入，避免重复注入。

- `publishDirectorDebugEntry(...)`
  - 保存导演调试记录。

### `services/fileImportService.js`

TXT 导入和章节队列生成。

负责：

- 用户选择 TXT 文件。
- 读取文本。
- 按章节正则拆分。
- 按字数重新分块。
- 生成 `AppState.memory.queue`。
- 更新章节队列 UI。

### `services/taskStateService.js`

工程包和任务快照。

负责：

- 导出工程包。
- 导入工程包。
- 自动恢复上次任务。
- 恢复章节队列、世界书、当前章节体验状态、处理进度。

### `services/settingsPersistenceService.js`

设置持久化。

负责：

- 从 localStorage 读取设置。
- 保存 API 配置、提示词、导演设置、分类设置。
- 兼容旧设置 key。

### `services/worldbookService.js`

世界书数据服务。

负责：

- 合并 AI 返回条目。
- 维护分类。
- 构造最终世界书对象。
- 统计条目。

### `services/worldbookRuntimeService.js`

世界书运行时查询。

常用于：

- 根据当前状态获取世界书。
- 获取所有分卷世界书。
- 给导出服务提供数据。

### `services/exportFormatService.js`

导出格式转换。

负责：

- 转成 SillyTavern 世界书格式。
- 转成角色卡绑定世界书格式。

### `services/importExportService.js`

导入导出用户操作。

负责：

- 导出角色卡。
- 导出世界书。
- 导入设置。
- 导出设置。
- 分卷导出。

### `services/rerollService.js`

重 Roll 服务。

负责：

- 重跑某章。
- 重跑某条世界书条目。
- 批量重 Roll。
- 读取历史结果。

### `services/repairService.js`

失败修复服务。

负责：

- 找出失败章节。
- 重新处理失败部分。
- 保存修复后的结果。

### `services/replaceAndCleanService.js`

文本替换和清理服务。

负责：

- 查找替换世界书内容。
- 清理标签。
- 删除重复片段。

### `services/mergeService.js`

条目合并底层服务。

负责：

- 判断同类条目。
- 合并条目内容。
- 别名识别。

### `services/mergeWorkflowService.js`

合并 UI 工作流。

负责：

- 打开整理条目弹窗。
- 打开别名合并界面。
- 手动合并条目。

### `services/importMergeService.js`

导入外部世界书或角色卡并合并进当前世界书。

### `services/memoryQueueActionsService.js`

章节队列操作。

负责：

- 选择起始章节。
- 合并到上一章/下一章。
- 删除章节。
- 多选删除。

### `services/categoryPersistenceService.js`

自定义分类持久化。

### `services/categoryLightService.js`

分类轻量配置保存。

### `services/entryConfigService.js`

世界书条目位置、深度、顺序、递归等配置管理。

### `services/processingStateService.js`

处理状态封装。

包括：

- 当前是否处理中。
- 停止状态。
- 全局信号量。

### `services/tokenMetricsService.js`

Token 统计和 UI 展示所需数据。

### `services/nameNormalizationService.js`

名称规范化，用于别名、合并、查重。

### `services/exportNameService.js`

导出文件名生成。

## 10. `ui/` 文件职责

### `ui/settingsPanel.js`

主模态框 HTML 构建。

包含：

- 页签导航。
- TXT 导入区。
- 章节正则区。
- 提示词编辑区。
- 设置区。
- 默认条目区。
- 章节队列区。
- 处理进度区。
- 生成结果区。
- 故事大纲区。
- 当前章节概览区。
- 导演调试区。

### `ui/modalStyles.js`

主 UI 的样式注入。

### `ui/modalController.js`

主模态框控制器。

负责：

- 创建主弹窗。
- 初始化状态。
- 恢复任务。
- 绑定滚动位置。
- 关闭弹窗。
- 页面退出时保存快照。

### `ui/modalLifecycle.js`

弹窗生命周期。

负责：

- 添加样式。
- 加载设置。
- 加载分类。
- 检查恢复状态。

### `ui/modalEventBinder.js`

把各种 UI 事件绑定函数统一装配到主弹窗。

### `ui/modalRuntimeFacade.js`

模态框运行 facade。

对外提供：

- `open`
- `close`
- `loadSavedSettings`
- `saveCurrentSettings`
- `bindModalEvents`

### `ui/eventBindings.js`

主 UI 事件绑定集合。

负责：

- 文件选择事件。
- 设置项修改事件。
- 提示词保存事件。
- 开始处理按钮。
- 停止按钮。
- 导出按钮。
- 页签切换。
- 搜索、替换、历史、合并等按钮。

### `ui/chapterExperienceView.js`

章节体验和导演调试 UI。

负责：

- 故事大纲页。
- 当前章节概览页。
- 节拍切换按钮。
- 当前章节开场白。
- 导演调试页。
- 导演调试记录展示和复制。

导演调试现在应展示三段：

1. 发给导演 AI API 的提示词原文。
2. 导演 AI API 原始返回。
3. 最终注入演员 AI 的提示词。

### `ui/memoryQueueView.js`

章节队列 UI。

负责：

- 渲染章节列表。
- 显示处理状态。
- 查看章节内容。
- 合并章节。
- 删除章节。
- 已处理结果查看。

### `ui/progressView.js`

处理进度 UI。

负责：

- 显示/隐藏章节队列区。
- 显示/隐藏进度区。
- 显示/隐藏结果区。
- 更新进度条。

### `ui/worldbookView.js`

世界书预览 UI。

负责：

- 渲染世界书分类和条目。
- 条目编辑。
- 条目删除。
- 手动合并入口。

### `ui/startButtonView.js`

开始按钮状态。

负责：

- 根据章节队列、处理状态、导演状态更新按钮文案。

### `ui/stopButtonView.js`

停止按钮状态和操作。

### `ui/settingsStateView.js`

设置 UI 状态同步。

### `ui/apiModeView.js`

API 模式切换。

包括主 AI 和导演 AI 的配置页签。

### `ui/modelActionsView.js`

模型拉取、测试相关 UI。

### `ui/messageChainView.js`

消息链配置 UI。

### `ui/chapterRegexView.js`

章节正则测试 UI。

### `ui/defaultEntriesView.js`

默认世界书条目 UI。

### `ui/categoryListView.js`

分类列表 UI。

### `ui/categoryEditorModal.js`

分类编辑弹窗。

### `ui/entryConfigModals.js`

条目配置弹窗。

### `ui/rerollModals.js`

重 Roll 弹窗和历史选择器。

### `ui/historyView.js`

历史记录和回滚 UI。

### `ui/searchModal.js`

查找 UI。

### `ui/replaceModal.js`

替换 UI。

### `ui/mergeModals.js`

合并和别名合并弹窗 HTML。

### `ui/helpModal.js`

帮助弹窗。

### `ui/promptPreviewModal.js`

提示词预览弹窗。

### `ui/createUiHelpers.js`

UI 帮助函数集合。

### `ui/createWorldbookViewRuntime.js`

创建世界书视图运行时。

### `ui/renderer.js`

通用渲染工具。

包括 HTML escape、列表渲染等。

### `ui/editorActionsFacade.js`

编辑类动作 facade。

### `ui/settingsActionsFacade.js`

设置类动作 facade。

## 11. `styles/`

### `styles/modal.css`

独立 CSS 文件。当前主 UI 大量样式也在 `ui/modalStyles.js` 中注入。

## 12. TXT 转世界书完整流程

### 12.1 打开插件面板

```text
SillyTavern 加载插件
  -> index.js
  -> 用户点击抽屉按钮
  -> txtToWorldbook/main.js 懒加载
  -> publicApi.open()
  -> modalRuntimeFacade.open()
  -> modalController.createModal()
  -> settingsPanel.buildModalHtml()
  -> modalEventBinder.bindModalEvents()
```

涉及文件：

- `index.js`
- `txtToWorldbook/main.js`
- `app/publicApi.js`
- `app/createShellRuntime.js`
- `ui/modalRuntimeFacade.js`
- `ui/modalController.js`
- `ui/settingsPanel.js`
- `ui/modalEventBinder.js`
- `ui/eventBindings.js`

### 12.2 导入 TXT

```text
用户选择 TXT 文件
  -> eventBindings.bindFileEvents()
  -> fileImportService.handleFileSelect()
  -> fileUtils 读取文件和编码
  -> fileImportService.splitContentIntoMemory()
  -> AppState.memory.queue 写入章节队列
  -> memoryQueueView.updateMemoryQueueUI()
  -> startButtonView.updateStartButtonState()
  -> MemoryHistoryDB.saveState()
```

涉及文件：

- `ui/eventBindings.js`
- `services/fileImportService.js`
- `core/fileUtils.js`
- `core/state.js`
- `ui/memoryQueueView.js`
- `ui/startButtonView.js`
- `infra/memoryHistoryDB.js`

### 12.3 开始世界书抽取

```text
用户点击“仅提取世界书”或相关开始按钮
  -> eventBindings.bindActionEvents()
  -> runtimeActionsFacade.handleStartConversion()
  -> processingService.handleStartProcessing()
  -> processingService.processMemoryChunk()
  -> promptService 构建世界书抽取提示词
  -> apiService.callAPI()
  -> AI 返回文本
  -> parserService.parseAIResponse()
  -> worldbookService 合并条目
  -> MemoryHistoryDB 保存 roll/history/state
  -> progressView.updateProgress()
  -> memoryQueueView.updateMemoryQueueUI()
  -> worldbookView.updateWorldbookPreview()
```

涉及文件：

- `ui/eventBindings.js`
- `app/runtimeActionsFacade.js`
- `services/processingService.js`
- `services/promptService.js`
- `services/apiService.js`
- `infra/apiCaller.js`
- `services/parserService.js`
- `services/worldbookService.js`
- `infra/memoryHistoryDB.js`
- `ui/progressView.js`
- `ui/memoryQueueView.js`
- `ui/worldbookView.js`

### 12.4 生成章节大纲和节拍

章节大纲与节拍主要在处理章节时生成或补齐。

```text
processingService
  -> 调用 AI 生成章节概览/剧本结构
  -> 写入 memory.chapterOutline
  -> 写入 memory.chapterScript.beats
  -> chapterExperienceView 渲染故事大纲和当前章节概览
```

涉及文件：

- `services/processingService.js`
- `services/directorService.js`
- `ui/chapterExperienceView.js`
- `core/state.js`

### 12.5 导出

```text
用户点击导出
  -> eventBindings.bindExportEvents()
  -> importExportService
  -> worldbookRuntimeService / worldbookService 获取数据
  -> exportFormatService 转换格式
  -> 浏览器下载 JSON
```

涉及文件：

- `ui/eventBindings.js`
- `services/importExportService.js`
- `services/worldbookRuntimeService.js`
- `services/worldbookService.js`
- `services/exportFormatService.js`
- `services/exportNameService.js`

## 13. 用户输入到演员 AI 输出的完整导演流程

这是当前最重要的实时链路。

### 13.1 用户发送消息

用户在 SillyTavern 输入消息并点击发送。

SillyTavern 开始准备生成，触发插件监听事件。

```text
SillyTavern 用户发送
  -> index.js 监听生成前事件
  -> getTxtToWorldbookApi()
  -> runDirectorBeforeGeneration(eventData)
```

涉及文件：

- `index.js`
- `txtToWorldbook/main.js`
- `app/publicApi.js`
- `services/directorService.js`

### 13.2 `index.js` 的导演闸门

`index.js` 负责决定什么时候调用导演。

它做的事：

- 确认 TXT 转世界书模块已经初始化。
- 确认导演功能开启。
- 防止同一轮重复处理。
- 在生成前调用：

```js
runDirectorBeforeGeneration(eventData)
```

`eventData.chat` 是 SillyTavern 即将发送给模型的消息数组。

### 13.3 导演服务读取当前章节和节拍

进入：

```text
services/directorService.js
  -> runDirectorBeforeGeneration(eventData)
```

它先读取：

- `AppState.experience.currentChapterIndex`
- `AppState.memory.queue[chapterIndex]`
- `memory.chapterScript.beats`
- `memory.chapterCurrentBeatIndex`

如果没有章节、没有节拍、导演关闭，就直接跳过。

### 13.4 清洗最近对话

当前代码会从两个来源合并读取：

1. SillyTavern 真实聊天：
   - `SillyTavern.getContext().chat`

2. 本轮生成上下文：
   - `eventData.chat`

然后统一过滤：

- `<Task>`
- `<thinking>`
- 演员自检文本。
- 导演握手文本。
- 执行单文本。
- 系统提示词。
- `秋青子，开始你的表演`
- 后拼的硬约束。

相关函数：

- `getGenerationChatSources(eventData)`
- `buildCleanDialogueEntries(eventData)`
- `getLatestUserMessage(eventData)`
- `getLatestAssistantMessage(eventData)`
- `getLatestDialogue(eventData)`

输出：

- `latestUserMessage`
  - 干净用户输入。
- `latestAssistantMessage`
  - 上一轮干净剧情正文。
- `latestDialogue`
  - 最近几轮干净剧情正文。

### 13.5 计算节拍模式和起笔锚点

相关函数：

```text
buildDirectionContext(...)
```

输入：

- 当前 beats。
- 当前锁定 beat index。
- 是否新节拍。
- 是否大跳转。
- 最近 AI 正文。
- 最近用户输入。

输出：

- `mode`
  - `new_beat`
  - `in_beat`

- `start_mode`
  - `jump`
  - `beat-head`
  - `continue`
  - `user`
  - `empty`

- `start_anchor`
  - 演员起笔锚点。

规则：

```text
大跳转:
  -> 用新节拍原文前 50 字

新入节拍:
  -> 用新节拍原文前 50 字

节拍中段且有干净 AI 正文:
  -> 用上一轮 AI 输出尾部 50 字

节拍中段但没有干净 AI 正文:
  -> 用用户输入作为临时锚点

都没有:
  -> 空锚点
```

### 13.6 构造导演 AI 请求

相关函数：

```text
buildDirectorPrompt(...)
```

主要内容来自：

- `constants.js` 中的 `defaultDirectorFrameworkPrompt`
- 当前章节标题。
- 当前章节大纲。
- 当前节拍原文。
- 起笔锚点。
- 最近干净 AI 正文。
- 最近干净用户输入。
- 最近干净对话。
- 节拍列表。
- 固定 stage index。

输出是发给导演 AI API 的 prompt。

### 13.7 调用导演 AI

```text
directorService.runDirectorBeforeGeneration()
  -> callDirectorAPI(prompt, chapterIndex + 1)
  -> apiService.callDirectorAPI()
  -> apiService.callTargetPrompt(..., 'director')
  -> apiService.callCustomAPI() 或 callSillyTavernAPI()
```

涉及文件：

- `services/directorService.js`
- `services/apiService.js`
- `infra/apiCaller.js`

导演 AI 应返回 JSON，结构大致：

```json
{
  "stage_idx": 0,
  "conflict_level": "normal",
  "conflict_reason": "",
  "conflict_strategy": "",
  "will_complete_this_last_turn": false,
  "will_complete_this_turn": false,
  "beat_complete_reason": "",
  "direction_script": {
    "action_chain": "动作A→动作B→动作C",
    "end": "临时收束点"
  }
}
```

注意：

- `direction_script.start` 不再由导演 AI 输出。
- 起点由系统的 `start_anchor` 注入。

### 13.8 解析和修正导演 JSON

相关函数：

- `extractJsonObject(response)`
- `normalizeDecision(...)`
- `normalizeDirectionScript(...)`
- `buildFallbackDecision(...)`

处理内容：

- 提取 JSON。
- 修正字段名兼容。
- 固定 `stage_idx`，不允许导演自己跳拍。
- 补齐缺失动作链。
- 过滤过长、混乱、塞了整段摘要的动作链段。
- 确认冲突等级。
- 确认本轮是否耗尽节拍。

### 13.9 构造演员执行单

相关函数：

```text
buildInjection(decision, beats)
```

输入：

- 规范化后的导演 decision。
- 当前 beats。
- `direction_context.start_anchor`

使用模板：

- `constants.js`
  - `defaultDirectorInjectionPrompt`

生成内容：

- 当前节拍事件梗概。
- 冲突控制块。
- 当前节拍小说原文。
- 起点。
- 动作链。
- 过程。
- 终点。
- 下一节拍预览。
- 演员执行硬规则。

注意：

- `DIRECTION_START` 使用系统计算出的 `start_anchor`。
- 不是导演 AI 自己写的 start。

### 13.10 注入到演员 AI

相关函数：

```text
stripExistingDirectorInjection(eventData.chat)
eventData.chat.unshift({...})
```

执行顺序：

```text
先删除旧导演注入
  -> 构造新的 system 消息
  -> unshift 到 eventData.chat 最前面
  -> SillyTavern 继续把 eventData.chat 发给演员 AI
```

注入消息标记：

```js
is_westworld_director: true
is_storyweaver_director: true
is_system: true
```

### 13.11 演员 AI 输出

演员 AI 收到：

- 原聊天上下文。
- 当前用户输入。
- 最前面的导演执行单 system 消息。

然后输出剧情正文。

### 13.12 生成后处理

SillyTavern 生成完成后，`index.js` 会调用：

```text
handleDirectorAfterGeneration(eventData)
```

它会：

- 检查本轮 AI 是否真的输出了新内容。
- 根据导演判定决定是否提示当前节拍完成。
- 更新 `AppState.experience` 中的状态。

## 14. 导演调试模块流程

导演调试记录由：

```text
directorService.publishDirectorDebugEntry(...)
```

写入：

```text
AppState.ui.directorDebugEntries
```

UI 展示在：

```text
ui/chapterExperienceView.js
```

当前定位是三段链路透明化：

1. 发给导演 AI API 的提示词原文。
2. 导演 AI API 原始返回。
3. 最终注入到演员 AI 的提示词。

这三个字段分别是：

- `directorPrompt`
- `directorRawResponse`
- `actorInjection`

## 15. 状态流转核心对象

### `AppState.memory.queue`

章节队列。

每个 memory item 大致包含：

- `title`
- `content`
- `processed`
- `failed`
- `result`
- `chapterTitle`
- `chapterOutline`
- `chapterScript`
- `chapterCurrentBeatIndex`
- `directorDecision`

### `AppState.worldbook.generated`

当前生成出的世界书。

结构通常是：

```js
{
  分类名: {
    条目名: {
      关键词: [],
      内容: ''
    }
  }
}
```

### `AppState.experience`

章节体验和导演状态。

常用字段：

- `currentChapterIndex`
- `currentBeatIndex`
- `lastChapterIdx`
- `lastBeatIdx`
- `directorLastDecision`
- `directorLastDecisionAt`
- `pendingBeatCompletionNotice`

### `AppState.settings`

设置。

包括：

- 主 API 配置。
- 导演 API 配置。
- 自定义提示词。
- 分类配置。
- 导演开关。
- 并行处理配置。

## 16. 两条最重要的文件流转顺序

### 16.1 TXT 转世界书

```text
index.js
  -> txtToWorldbook/main.js
  -> app/createApp.js
  -> core/state.js
  -> infra/memoryHistoryDB.js
  -> ui/modalController.js
  -> ui/settingsPanel.js
  -> ui/eventBindings.js
  -> services/fileImportService.js
  -> ui/memoryQueueView.js
  -> app/runtimeActionsFacade.js
  -> services/processingService.js
  -> services/promptService.js
  -> services/apiService.js
  -> infra/apiCaller.js
  -> services/parserService.js
  -> services/worldbookService.js
  -> infra/memoryHistoryDB.js
  -> ui/progressView.js
  -> ui/worldbookView.js
```

### 16.2 用户输入到演员 AI 输出

```text
用户在 SillyTavern 输入
  -> index.js 捕获生成前事件
  -> txtToWorldbook/main.js 暴露的 public API
  -> services/directorService.js/runDirectorBeforeGeneration()
  -> directorService 清洗真实聊天正文
  -> directorService/buildDirectionContext()
  -> directorService/buildDirectorPrompt()
  -> services/apiService.js/callDirectorAPI()
  -> 导演 AI 返回 JSON
  -> directorService/extractJsonObject()
  -> directorService/normalizeDecision()
  -> directorService/buildInjection()
  -> eventData.chat.unshift(system 导演执行单)
  -> SillyTavern 把消息发给演员 AI
  -> 演员 AI 输出剧情正文
  -> index.js 捕获生成后事件
  -> directorService/handleDirectorAfterGeneration()
```

## 17. 排查问题时优先看哪里

### 导演起点错

看：

- `services/directorService.js`
  - `getLatestAssistantMessage`
  - `buildCleanDialogueEntries`
  - `buildDirectionContext`
  - `start_anchor`
  - `start_mode`

再看导演调试三段：

- 发给导演的 prompt。
- 导演原始返回。
- 注入演员的 prompt。

### 动作链乱

看：

- `services/directorService.js`
  - `normalizeActionSegment`
  - `splitActionChain`
  - `buildDefaultDirectionScript`
  - `normalizeDirectionScript`

### 导演调试里混入演员提示词

看：

- `services/directorService.js`
  - `isPromptLikeDialogue`
  - `stripInjectedUserSuffix`
  - `buildCleanDialogueEntries`

### 章节队列显示错

看：

- `ui/progressView.js`
- `ui/chapterExperienceView.js`
- `ui/settingsPanel.js`

### 设置保存不对

看：

- `services/settingsPersistenceService.js`
- `ui/settingsPanel.js`
- `ui/eventBindings.js`

### 任务恢复不对

看：

- `services/taskStateService.js`
- `infra/memoryHistoryDB.js`
- `ui/modalController.js`

## 18. 修改代码时的注意点

1. 中文文件和长中文字符串要小心编码。
2. `core/constants.js` 可能涉及 UTF-8 BOM 或中文长文本，修改后要验证。
3. PowerShell 里不要用 `python -c` 传长中文字符串。
4. `eventData.chat` 不一定是纯聊天记录，它可能是即将发给模型的 prompt 链。
5. 导演调试的“原始返回”应该是 API 返回原文，不是解析后的 decision。
6. `direction_script.start` 不应由导演 AI 决定，起点应由系统锚点注入。
7. 节拍中段续写应优先用干净 AI 正文尾部 50 字。
8. 新入节拍应使用当前节拍原文开头，不应融合上一轮尾部。

